<!DOCTYPE html>
<html lang="es">
<head>
<meta charset="UTF-8"/>
<meta name="viewport" content="width=device-width, initial-scale=1.0"/>
<title>{{churchName}} - {{worshipType}}</title>
<link rel="preconnect" href="https://fonts.googleapis.com"/>
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin/>
<link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Cormorant+Garamond:ital,wght@0,400;0,600;0,700;1,400&family=DM+Sans:wght@300;400;500;600&display=swap"/>
<style>
  *{box-sizing:border-box;margin:0;padding:0}
  html,body{
    width:100%;height:100%;
    font-family:'DM Sans',Helvetica,Arial,sans-serif;
    background:#fff;color:#1B2D5B;
    -webkit-print-color-adjust:exact;
    print-color-adjust:exact;
  }

  /* ═══════════════════════════════════════════
     FULL-PAGE FLYER  —  Letter 8.5 × 11 in
     ═══════════════════════════════════════════ */
  .page{
    width:100%;
    min-height:100vh;
    display:flex;
    flex-direction:column;
    background:#fff;
  }

  /* ─── HEADER ─── */
  .hdr{
    background:linear-gradient(135deg,#1B2D5B 0%,#253E78 50%,#1B2D5B 100%);
    padding:2.8rem 3rem 2rem;
    position:relative;
    overflow:hidden;
  }
  /* decorative circles */
  .hdr::before{
    content:'';position:absolute;top:-60px;right:-60px;
    width:200px;height:200px;
    background:rgba(200,168,75,.07);border-radius:50%;
  }
  .hdr::after{
    content:'';position:absolute;bottom:-30px;left:-40px;
    width:140px;height:140px;
    background:rgba(200,168,75,.05);border-radius:50%;
  }

  .hdr-inner{
    display:flex;align-items:center;gap:1.5rem;
    position:relative;z-index:1;
  }
  .hdr-text{
    flex:1;display:flex;flex-direction:column;
    align-items:center;text-align:center;
  }
  .hdr-name{
    font-family:'Cormorant Garamond',Georgia,serif;
    font-size:1.75rem;font-weight:700;color:#fff;
    line-height:1.15;letter-spacing:.03em;
  }
  .hdr-sub{
    font-size:.85rem;color:#E8C96A;
    letter-spacing:.06em;margin-top:6px;
  }
  .hdr-loc{
    font-size:.72rem;color:rgba(255,255,255,.45);margin-top:4px;
  }

  .logo{flex-shrink:0;width:68px;height:68px}
  .logo img{
    width:68px;height:68px;border-radius:12px;
    object-fit:contain;
    border:2.5px solid rgba(200,168,75,.45);
    background:rgba(255,255,255,.08);
  }
  .logo-fb{
    width:68px;height:68px;border-radius:12px;
    background:rgba(255,255,255,.08);
    border:2.5px solid rgba(200,168,75,.4);
    display:flex;align-items:center;justify-content:center;
    font-size:2rem;color:rgba(255,255,255,.55);
  }

  /* ─── GOLD BAND ─── */
  .gold{
    height:5px;
    background:linear-gradient(90deg,#B8963F,#E8C96A 30%,#F5DFA0 50%,#E8C96A 70%,#B8963F);
  }

  /* ─── BODY CONTENT ─── */
  .body{
    flex:1;
    display:flex;flex-direction:column;
    align-items:center;
    padding:1.8rem 2.5rem 1rem;
  }

  /* ─── BADGE ─── */
  .badge{
    display:inline-block;
    background:linear-gradient(135deg,#C8A84B,#E0BE5A);
    color:#1B2D5B;
    font-family:'Cormorant Garamond',Georgia,serif;
    font-size:1.15rem;font-weight:700;
    padding:8px 32px;border-radius:30px;
    letter-spacing:.07em;text-transform:uppercase;
    box-shadow:0 2px 8px rgba(200,168,75,.25);
    margin-bottom:1rem;
  }

  /* ─── DATE / TIME ─── */
  .dt{text-align:center;margin-bottom:.8rem}
  .dt-date{
    font-family:'Cormorant Garamond',Georgia,serif;
    font-size:1.15rem;color:#1B2D5B;font-weight:600;
  }
  .dt-time{font-size:.82rem;color:#6B7280;margin-top:3px}

  /* ─── ORNAMENT ─── */
  .orn{
    display:flex;align-items:center;gap:10px;
    width:100%;max-width:420px;
    margin:0 auto .8rem;
  }
  .orn-line{flex:1;height:1px;background:linear-gradient(90deg,transparent,#D1D5DB,transparent)}
  .orn-d{display:flex;gap:5px}
  .orn-d i{
    display:block;width:6px;height:6px;
    background:#C8A84B;transform:rotate(45deg);
  }
  .orn-d i:first-child,.orn-d i:last-child{opacity:.55}
  .orn-d i:nth-child(2){opacity:1}

  /* ─── SECTION TITLE ─── */
  .sec-title{
    font-size:.7rem;font-weight:700;
    letter-spacing:.18em;text-transform:uppercase;
    color:#1B2D5B;margin-bottom:.9rem;
  }

  /* ─── TABLE ─── */
  .tbl{width:100%;max-width:460px;margin-bottom:auto}

  .row{
    display:flex;align-items:center;
    padding:11px 14px;
    border-radius:8px;
    margin-bottom:4px;
    min-height:46px;
  }
  .row-even{background:#F3F4F6}
  .row-odd{background:#fff;border:1px solid #EEF0F3}

  .row-num{
    flex-shrink:0;width:26px;height:26px;
    background:#1B2D5B;color:#E8C96A;
    border-radius:6px;
    font-size:.7rem;font-weight:700;
    display:flex;align-items:center;justify-content:center;
    margin-right:12px;
  }
  .row-role{
    flex-shrink:0;width:150px;
    font-size:.85rem;font-weight:600;color:#1B2D5B;
  }
  .row-person{
    flex:1;
    font-family:'Cormorant Garamond',Georgia,serif;
    font-size:1.05rem;font-weight:600;
    color:#111827;font-style:italic;
  }
  .row-empty{
    flex:1;color:#CBD5E1;
    font-size:.8rem;letter-spacing:.04em;
  }

  /* ─── VERSE ─── */
  .verse{
    text-align:center;
    padding:1.2rem 2rem .6rem;
    border-top:1px solid #F0F1F3;
    margin-top:1.2rem;
    width:100%;max-width:460px;
  }
  .verse p{
    font-family:'Cormorant Garamond',Georgia,serif;
    font-size:.95rem;font-style:italic;
    color:#6B7280;line-height:1.55;margin:0;
  }

  /* ─── FOOTER ─── */
  .ftr{
    background:linear-gradient(135deg,#1B2D5B 0%,#253E78 100%);
    padding:14px 3rem;
    text-align:center;
    margin-top:auto;
  }
  .ftr span{
    font-size:.72rem;font-weight:600;
    color:rgba(255,255,255,.6);
    letter-spacing:.1em;text-transform:uppercase;
  }

  @media print{
    html,body{height:auto}
    .page{min-height:auto}
    .gold{print-color-adjust:exact;-webkit-print-color-adjust:exact}
  }
</style>
</head>
<body>
<div class="page">

  <!-- ══════ HEADER ══════ -->
  <div class="hdr">
    <div class="hdr-inner">
      <div class="hdr-text">
        <div class="hdr-name">{{churchName}}</div>
        {{#if subtitle}}<div class="hdr-sub">{{subtitle}}</div>{{/if}}
        {{#if location}}<div class="hdr-loc">{{location}}</div>{{/if}}
      </div>
      <div class="logo">
        {{#if logoUrl}}
        <img src="{{logoUrl}}" alt="Logo"/>
        {{else}}
        <div class="logo-fb">&#10013;</div>
        {{/if}}
      </div>
    </div>
  </div>

  <!-- ══════ GOLD BAND ══════ -->
  <div class="gold"></div>

  <!-- ══════ BODY ══════ -->
  <div class="body">

    <!-- Badge -->
    <div class="badge">{{worshipType}}</div>

    <!-- Date & Time -->
    <div class="dt">
      <div class="dt-date">{{formattedDate}}</div>
      {{#if formattedTime}}<div class="dt-time">{{formattedTime}}</div>{{/if}}
    </div>

    <!-- Ornament -->
    <div class="orn">
      <div class="orn-line"></div>
      <div class="orn-d"><i></i><i></i><i></i></div>
      <div class="orn-line"></div>
    </div>

    <!-- Section Title -->
    <div class="sec-title">Orden del Culto</div>

    <!-- Table -->
    <div class="tbl">
      {{#each assignments}}
      <div class="row {{#if (eq (mod @index 2) 0)}}row-even{{else}}row-odd{{/if}}">
        <div class="row-num">{{padId (addOne @index)}}</div>
        <div class="row-role">{{roleName}}</div>
        {{#if personName}}
        <div class="row-person">{{personName}}</div>
        {{else}}
        <div class="row-empty">&mdash; &mdash; &mdash; &mdash; &mdash; &mdash; &mdash;</div>
        {{/if}}
      </div>
      {{/each}}
    </div>

    <!-- Verse -->
    {{#if verse}}
    <div class="verse">
      <p>{{verse}}</p>
    </div>
    {{/if}}

  </div>

  <!-- ══════ FOOTER ══════ -->
  <div class="ftr">
    <span>{{churchNameUpper}}</span>
  </div>

</div>
</body>
</html>
